import type { ButtonComponent, ButtonVariant } from "./types";

const DEFAULT_COMPONENT: ButtonComponent = "button";
const DEFAULT_VARIANT: ButtonVariant = "filled";

export { DEFAULT_COMPONENT, DEFAULT_VARIANT };
